#include <stdio.h>
#include<string.h>
int main() {
char gopal[10];
printf("entre your string\n");
scanf("%s",gopal);
printf("before concact%s\n",gopal);
strcat(gopal,"legend");
printf("after concact%s/n",gopal);

    return 0;
}
